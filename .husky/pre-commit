echo "hello from pre-commit"
pwd
cd ghrunner

#run lint check
make lint

#run fmt check
diff=$(gofmt -l -s -d .)
if [ -n "$diff" ]; then
    echo "Files are not formatted:"
    echo "$diff"
    exit 1
else
    echo "All files are properly formatted."
fi
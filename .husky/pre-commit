
# # Create a virtual environment
# source $(pwd)/.husky/venv/bin/activate
# pip install -r $(pwd)/.husky/requirements.txt
# # Call the Python script to check if commit message references an issue existing in repository
# python $(pwd)/.husky/validate-issue.py


#!/bin/bash

commit_msg_file=$1
commit_msg=$(cat $commit_msg_file)

# Define a regular expression to match your desired commit message format

echo $commit_msg

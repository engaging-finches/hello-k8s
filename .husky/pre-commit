
# Create a virtual environment
source $(pwd)/.husky/venv/bin/activate
pip install -r $(pwd)/.husky/requirements.txt

# get current commit message
commit_msg_file=$1
commit_msg=$(cat $commit_msg_file)
echo $1
# Call the Python script and pass it current commit message
# to check if commit message references an issue existing in repository
# python $(pwd)/.husky/validate-issue.py --message $commit_msg


